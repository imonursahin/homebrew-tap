# Homebrew Tap — Archo

Install [Archo](https://github.com/imonursahin/archo):

```bash
brew install --cask imonursahin/tap/archo
```

The cask strips the quarantine flag on install, so the app opens normally even
though it is unsigned.
